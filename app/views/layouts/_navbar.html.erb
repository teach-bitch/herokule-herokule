<nav class="navbar navbar-expand-lg bg-white fixed-top" >
    <div class="container">
        <div class="navbar-translate">
            <a class="navbar-brand text-primary" href="/products" rel="tooltip" title="" data-placement="bottom">
                Chat Perlipopette
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navigation" aria-controls="navigation-index" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-bar bar1"></span>
                <span class="navbar-toggler-bar bar2"></span>
                <span class="navbar-toggler-bar bar3"></span>
            </button>
        </div>
        <div class="collapse navbar-collapse" data-nav-image="" data-color="orange">
            <ul class="navbar-nav ml-auto">
              <% if user_signed_in? %>
                <li>
                  <a href="<%= basket_path(@current_basket.id) %>" class="nav-link text-primary">
                    <i class="now-ui-icons shopping_cart-simple"></i> Mon Panier
                  </a>
                </li>
              <% end %>
                <li class="nav-item dropdown">
                    <a href="" class="nav-link dropdown-toggle text-primary" id="navbarDropdownMenuLink" data-toggle="dropdown">
                        <i class="now-ui-icons users_single-02" aria-hidden="true"></i>
                        <% if user_signed_in? %>
                          <p>Bonjour, <strong><%= current_user.username %></strong></p>
                        <% else %>
                          <p>Compte</p>
                        <% end %>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
                      <%= render 'devise/menu/login_items' %>
                      <%= render 'devise/menu/registration_items' %>
                      <%= render 'layouts/bill_btn' %>
                  </div>
                </li>
            </ul>
        </div>
    </div>
</nav>
