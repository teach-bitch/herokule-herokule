<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Salut <%= @user.first_name %> <%= @user.last_name %>, merci pour cet achat !</h1>
    <%= puts "je rentre dans bill_email.html.erb apres @user.email" %>

    <p>
      Tu as fait l'achat des produits suivant :
      <ul>
      <% @facturation.basket.line_items.all.each do |line| %>
      <li>
      <p><%= line.product.title %> : <%= line.product.price %> € X <%= line.quantity %> = <%=line.product.price*line.quantity%> €</P>
      </li>
      <% end %>
      </ul>
    </p>

      <%= puts "je rentre dans bill_email.html.erb apres @facturation.basket.product.title" %>
    <p>
      pour la somme de : <%= @facturation.price %> €.
            <%= puts "je rentre dans bill_email.html.erb apres @facturation.price" %>
    </p>
    <p>
      Pour accéder à ton espace client, connecte-toi via : <%= @url %>.
    </p>
    <p> À très vite sur catpeach.fr !</p>
  </body>
</html>
